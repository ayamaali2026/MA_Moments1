"use strict";document.addEventListener("DOMContentLoaded",()=>{const root=document.getElementById("wishlistItems");function render(){const a=maGetArray(MA_WISHLIST_KEY);root.innerHTML=a.length?a.map(i=>`<article class="saved-item" data-id="${i.id}"><a href="product.html?id=${i.id}"><img src="${i.image}"></a><div><a href="product.html?id=${i.id}"><h2>${i.name}</h2></a><strong>${i.price} ₪</strong></div><button data-remove>حذف</button></article>`).join(""):'<section class="empty-page"><h1>العناصر المفضلة</h1><p>لا توجد عناصر مفضلة حاليًا.</p></section>';root.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{maSetArray(MA_WISHLIST_KEY,a.filter(x=>x.id!==b.closest('.saved-item').dataset.id));render()})}render()});
