"use strict";document.addEventListener("DOMContentLoaded",()=>{const root=document.getElementById("cartItems");function render(){const a=maGetArray(MA_CART_KEY);root.innerHTML=a.length?a.map(i=>`<article class="saved-item" data-key="${i.key}"><img src="${i.image}"><div><h2>${i.name}</h2><p>${i.selectedColor||''} ${i.selectedSize||''}</p><strong>${i.price} ₪ × ${i.quantity}</strong></div><button data-remove>حذف</button></article>`).join(""):'<section class="empty-page"><h1>سلة التسوق</h1><p>السلة فارغة حاليًا.</p></section>';root.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{maSetArray(MA_CART_KEY,a.filter(x=>x.key!==b.closest('.saved-item').dataset.key));render()})}render()});
